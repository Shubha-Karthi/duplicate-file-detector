duplicate-file-detector
A Python-based duplicate file detector that scans folders and identifies duplicate files using MD5 hashing. The project uses file handling, hashing, and optimized comparison techniques to efficiently detect files with identical content across directories.
